# Database connection (preferred: split fields; auto URL-encoding for username/password)
username='dbauser'
password='JHKDSJrShkjSsdfsd348958234/.$#@54'
# e.g. 127.0.0.1 或远程数据库地址
host='localhost'
port=3306
database='automatica'
LOG_DB_CREDS=true
# Optional overrides
# driver='mysql+pymysql'
# charset='utf8mb4'

# Alternatively, you may set a full SQLAlchemy URL directly (overrides above when parts are missing):
# DATABASE_URL=mysql+pymysql://user:password@host:3306/automatica?charset=utf8mb4

# CORS origins for static site (comma separated). For GitHub Pages / 生产域名:
# CORS_ALLOW_ORIGINS=https://<username>.github.io
CORS_ALLOW_ORIGINS=https://order.wen-xc.site

# JWT and Admin
JWT_SECRET=change-this-secret
JWT_EXPIRE_HOURS=12
ADMIN_USERNAME=admin
# Prefer hash in production. Generate with /auth.get_password_hash utility if needed.
# ADMIN_PASSWORD_HASH=
# For local/dev only:
ADMIN_PASSWORD=admin123

# Rate for computed shipping fee when order.shipping_fee is NULL
RATE_PER_KG=0
